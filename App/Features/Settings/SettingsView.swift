import SwiftUI

struct SettingsView: View {
    var body: some View {
        Form {
            Section("General") {
                Text("DNS / 局域网绕过 / 关于 — 待实现")
            }
        }
        .navigationTitle("Settings")
    }
}
