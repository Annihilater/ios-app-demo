import SwiftUI

struct NodeEditView: View {
    var body: some View {
        Form {
            // TODO: 名称、协议、服务器、端口、凭证
            Text("Node editor placeholder")
        }
        .navigationTitle("Edit Node")
    }
}
